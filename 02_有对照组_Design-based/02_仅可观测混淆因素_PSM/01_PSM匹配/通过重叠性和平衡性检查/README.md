# PSM 通过重叠性和平衡性检查 ✅

## 状态

- ✅ **重叠性检查通过**：处理组与对照组倾向得分分布有充分重叠
- ✅ **样本损失可接受**：匹配后保留了足够样本量
- ✅ **平衡性检查通过**：匹配后所有关键协变量的 SMD < 阈值

## 结论

PSM 匹配结果**可信**，可将匹配后的样本用于后续分析（如直接比较均值、OLS 回归等），估计 ATT。

## 后续步骤

进入 **稳健性检验** 阶段（见 `03_稳健性检验/`）：
- 替换匹配算法（如从 1:1 改为 Kernel 匹配）
- 替换倾向得分模型（如从 Logit 改为更复杂模型）
- 安慰剂检验
- 敏感性分析（Rosenbaum Bounds）

